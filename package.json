{
  "name": "gatsby-example-mobx",
  "description": "A Gatsby example site using client-side routes and mobx",
  "version": "1.0.0",
  "author": "Jez Templeton <jez.templeton@bom.gov.au",
  "dependencies": {
    "gatsby": "1.0.0-beta.6",
    "gatsby-link": "1.0.0-beta.4",
    "gatsby-plugin-react-helmet": "1.0.0-beta.1",
    "mobx": "^3.2.1",
    "mobx-persist": "^0.3.3",
    "mobx-react": "^4.2.2",
    "mobx-react-devtools": "^4.2.15"
  },
  "devDependencies": {
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "gh-pages": "^0.12.0"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"pages/*.js\" \"utils/*.js\" \"wrappers/*.js\" \"html.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
